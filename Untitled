Please review and optimize the repository with the following updates and guidelines.

### Task 1: Cloud Run Flags as Config in CI

You already added a commented "Platform/infrastructure" block to `config.yaml` (min_instances, max_instances, cpu, memory, timeout, concurrency, etc.). Now, the next step is:

- **Make Cloud Run flags more config-like**:  
  - Wire these values into `gcloud run deploy` in the CI pipeline.
  - Read them in CI (similar to how values are read from `terraform-output.json`) or pass them as static flags if there's no need for per-environment variance yet.
  - The values should be **environment-specific**. Do **not combine** them for all environments.
  - Values like min_instances, max_instances, cpu, memory, timeout, concurrency, etc., should be **separate** for each environment (dev, staging, production) and must adjust based on the environment's **usage, traffic, and consumption**.
  
### Task 2: Explicit Cloud Run Documentation in README

Add a section in the **README** (under **"What this repo does"** or **"CI/CD"**) that clearly describes the Cloud Run setup:

- **Infra (Terraform)**:  
  - Artifact Registry, BigQuery, GCS, IAM, etc. are managed by Terraform.  
  - Outputs from Terraform are used in CI/CD for app deployment.

- **App (CI/CD)**:  
  - Cloud Run service is created/updated **exclusively** via `gcloud run deploy` in the deploy job.
  - Terraform does **not** touch Cloud Run.
  
This is a clarification to match your actual implementation and prevent confusion in the future.

### Task 3: Log Cloud Run Deploy Flags in CI Output

Right now, the `gcloud run deploy` command is visible in CI, but:

- **Log Cloud Run flags in CI output**:
  - Add comments above the `gcloud run deploy` line in the CI YAML to explain what each flag does, especially when scaling or tuning values like `min_instances`, `max_instances`, `cpu`, `memory`, `timeout`, and `concurrency`.
  - This is **documentation** and helps future maintainers understand the configuration, even if it doesn’t affect the architecture.

### Summary

1. **Refactor Cloud Run flags** in the CI pipeline to make them more **configurable** and environment-specific, based on the usage, traffic, and consumption of each environment.
2. **Document** the current architecture clearly in the **README**, focusing on the separation of infra (Terraform) and app (Cloud Run CI/CD).
3. Add **CI logs** above the `gcloud run deploy` command to explain the deploy flags and help future developers understand the choices made.

Please implement the changes and review the entire repo for any inconsistencies, especially around the separation of infrastructure vs. app concerns.